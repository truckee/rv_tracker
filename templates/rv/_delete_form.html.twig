<form method="post" action="{{ path('r_v_delete', {'id': r_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r_v.id) }}">
    <button class="btn">Delete</button>
</form>
